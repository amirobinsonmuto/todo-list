import { itemArr } from './todo-item-factory';
import { generateItemCard } from '../helpers/generate-item-card'

function displayTodoItemCards() {
    itemArr.forEach(item => generateItemCard(item));
}

export { displayTodoItemCards }