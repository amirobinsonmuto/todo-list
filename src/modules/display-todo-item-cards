import { itemArr } from './todo-item-factory';
import { displayItemCard } from '../helpers/generate-item-card'

function displayTodoItemCards() {
    itemArr.forEach(item => displayItemCard(item));
}

export { displayTodoItemCards }